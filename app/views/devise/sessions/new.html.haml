%section.clearfix.inner_body
  .pc.cm
    #msg
      = ApplicationDecorator.flash_message
    = form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "mt20 form_box p20 clearfix log_box cm" }) do |f|
      -# %aside.fl.w50.mt10
      %article.p10
        %h4 Login
        %p Login to BuddyPix with your email, Facebook, or Google+ account.
      .ph10
        = hidden_field_tag "invitation_token", @invitation_token if @invitation_token
      .ph10
        = f.email_field :email, autofocus: true, placeholder: "Email Id"
      .p10.mt10.ar
        = f.password_field :password, autocomplete: "off", placeholder: "Password"
        = link_to "Forgot Password?", new_password_path(resource_name), class: "mt10 inline"
      .p10
        = f.submit "Login"
      .box_link.p10.ac.fl.w50.mt10
        = link_to content_tag(:i, "", class: "fa fa-facebook") + "Facebook Login", user_omniauth_authorize_path(:facebook), class: "fb"
      .box_link.p10.ac.fl.w50.mt10
        = link_to content_tag(:i, "", class: "fa fa-google") + "Google Login", user_omniauth_authorize_path(:google_oauth2), class: "g"
      %p.mt10.fl.w100.ac
        %strong
          Don't have an account?
          = link_to "Sign Up!", new_user_registration_path

